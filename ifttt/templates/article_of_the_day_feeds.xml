<?xml version="1.0" encoding="UTF-8"?>
<rss version="2.0">
    <channel>
        <title>Artifle of the Day feeds</title>
        <description>List of articles of the day for Wikipedia.</description>

        <language>en-us</language>
        {% for feeds in data %}
            <item>
                <created_at>{{feeds.created_at}}</created_at>
                <entry_id>{{feeds.entry_id}}</entry_id>
                <media_url>{{feeds.media_url}}</media_url>
                <summary>{{feeds.summary}}</summary>
                <title>{{feeds.title}}</title>
                <link>{{feeds.url}}</link>
            </item>
        {% endfor %}
    </channel>
</rss>